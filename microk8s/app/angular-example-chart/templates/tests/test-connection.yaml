apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "angular-example.fullname" . }}-test-connection"
  labels:
{{ include "angular-example.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "angular-example.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
